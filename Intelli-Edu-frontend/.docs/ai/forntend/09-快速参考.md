# 快速参考（AI 开发速查）

## 项目速览

```
智慧教育平台 (Vue 3 + Vite + Pinia + Element Plus)
├── 学生端：课程学习、AI助教、作业、考试、知识图谱
├── 教师端：课程管理、班级管理、题库管理、作业/考试发布
└── 系统管理：用户、角色、菜单、部门
```

## 常用路径速查

| 要找什么 | 路径 |
|---------|------|
| 路由配置 | `src/router/index.js` |
| 状态管理 | `src/store/modules/` |
| API 接口 | `src/api/` |
| 全局组件 | `src/components/` |
| 页面视图 | `src/views/` |
| 布局组件 | `src/layout/` |
| 工具函数 | `src/utils/` |
| 自定义指令 | `src/directive/` |
| 全局样式 | `src/assets/styles/` |
| SVG 图标 | `src/assets/icons/svg/` |
| 项目配置 | `src/settings.js` |
| 环境变量 | `.env.development` |
| Vite 配置 | `vite.config.js` |

## 组件开发模板

```vue
<script setup lang="ts">
// 自动导入：ref, computed, onMounted, useRoute, useRouter 等

import api from '@/api/index.js'
const { CourseController } = api

const { proxy } = getCurrentInstance()
const route = useRoute()
const router = useRouter()

// 响应式数据
const loading = ref(false)
const list = ref([])
const total = ref(0)
const queryParams = reactive({
  pageNum: 1,
  pageSize: 10
})

// 方法
async function getList() {
  loading.value = true
  try {
    const res = await CourseController.getCourseList(queryParams)
    list.value = res.data.data.list
    total.value = res.data.data.total
  } finally {
    loading.value = false
  }
}

// 生命周期
onMounted(() => {
  getList()
})
</script>

<template>
  <div class="app-container">
    <el-table v-loading="loading" :data="list">
      <!-- 表格列 -->
    </el-table>
    <Pagination :total="total" v-model:page="queryParams.pageNum"
                v-model:limit="queryParams.pageSize" @pagination="getList" />
  </div>
</template>

<style scoped lang="scss">
</style>
```

## Store 使用速查

```javascript
// 用户 Store
import useUserStore from '@/store/modules/user'
const userStore = useUserStore()
userStore.token
userStore.userInfo
await userStore.login({ username, password })
await userStore.logOut()

// 权限 Store
import usePermissionStore from '@/store/modules/permission'
const permissionStore = usePermissionStore()
permissionStore.sidebarRouters

// 设置 Store
import useSettingsStore from '@/store/modules/settings'
const settingsStore = useSettingsStore()
settingsStore.tagsView
settingsStore.sidebarLogo
```

## API 调用速查

```javascript
import api from '@/api/index.js'

const {
  authController,      // 登录认证
  userController,      // 用户管理
  CourseController,    // 课程管理
  ChapterController,   // 章节管理
  SectionController,   // 小节管理
  ClassController,     // 班级管理
  InnerController,     // 内部接口
  coResourceController // 资源管理
} = api

// AI 接口（需单独导入）
import aiApi from '@/api/ai/index.ts'
const { aiCourseController, chatListController, fileUploadController } = aiApi

// 示例
const res = await CourseController.getCourseList(params)
const data = res.data.data
```

## 权限指令速查

```vue
<!-- 按钮权限 -->
<el-button v-hasPermi="['system:user:add']">新增</el-button>

<!-- 角色权限 -->
<el-button v-hasRole="['admin']">管理</el-button>
```

## 全局属性速查

```javascript
const { proxy } = getCurrentInstance()

proxy.$parseTime(new Date())           // 格式化日期
proxy.$resetForm('queryForm')          // 重置表单
proxy.$handleTree(data, 'id')          // 构建树
proxy.$addDateRange(params, dates)     // 添加日期范围
proxy.$selectDictLabel(dict, value)    // 字典标签
proxy.$download(url, params, filename) // 下载文件
proxy.$auth.hasPermi('system:user:add') // 权限判断
proxy.$auth.hasRole('admin')           // 角色判断
```

## 路由跳转速查

```javascript
const router = useRouter()
const route = useRoute()

// 跳转
router.push('/course/LearningCourses')
router.push({ path: `/course/LearningCourses/CourseDetails/${id}` })

// 获取参数
route.params.id
route.query.type
route.meta.title
```

## 环境变量速查

| 变量 | 值 |
|------|-----|
| `import.meta.env.VITE_APP_TITLE` | 智慧教育平台 |
| `import.meta.env.VITE_APP_ENV` | development / production |
| `import.meta.env.VITE_APP_BASE_API` | /api |

## 开发命令速查

```bash
npm run dev           # 开发（端口 80，host: true，自动打开浏览器）
npm run build:prod    # 生产打包（静态资源输出到 dist/assets/）
npm run build:stage   # 预发布打包
npm run openapi       # 根据 OpenAPI 规范生成 API 代码到 src/services/
npm run preview       # 预览生产构建
```

## 后端服务端口速查

| 服务 | 端口 | 代理路径 |
|------|------|---------|
| AI 服务 | 8896 | /api/ai |
| 用户服务 | 8891 | /api/user/* |
| 资源服务 | 8892 | /api/resource/* |
| 课程服务 | 8893 | /api/course |

## 文件命名规范速查

| 类型 | 命名方式 | 示例 |
|------|---------|------|
| 页面组件 | PascalCase + Index | `CourseExamsIndex.vue` |
| 公共组件 | PascalCase | `Pagination.vue` |
| Store | use + camelCase | `useUserStore` |
| API 文件 | camelCase | `course.ts` |
| 工具函数 | camelCase | `parseTime` |
| 路由 name | PascalCase | `CourseExamsLearning` |
