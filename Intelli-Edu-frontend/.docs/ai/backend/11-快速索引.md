# 前端 AI 开发指南 - 快速索引

## 1. 常用代码片段

### 1.1 页面模板

```vue
<template>
  <div class="page-name">
    <div class="page-header">
      <h2>页面标题</h2>
      <el-button type="primary" @click="handleAction">操作</el-button>
    </div>

    <!-- 筛选栏 -->
    <div class="filter-bar">
      <el-input v-model="queryParams.keyword" placeholder="搜索" @keyup.enter="fetchData" />
      <el-button type="primary" @click="fetchData">查询</el-button>
      <el-button @click="resetQuery">重置</el-button>
    </div>

    <!-- 数据表格 -->
    <el-table :data="list" v-loading="loading">
      <el-table-column prop="name" label="名称" />
      <el-table-column label="操作" width="200">
        <template #default="{ row }">
          <el-button @click="handleEdit(row)">编辑</el-button>
          <el-button type="danger" @click="handleDelete(row.id)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="queryParams.current"
      v-model:page-size="queryParams.pageSize"
      :total="total"
      @change="fetchData"
    />
  </div>
</template>

<script setup lang="ts">
import { reactive, ref, onMounted } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useCrud } from '@/composables/useCrud'

const { list, total, loading, queryParams, fetchList, handleDelete } = useCrud({
  listApi: apiModule.listApi,
  deleteApi: apiModule.deleteApi
})

const fetchData = fetchList

const resetQuery = () => {
  queryParams.current = 1
  queryParams.keyword = ''
  fetchData()
}

onMounted(fetchData)
</script>
```

### 1.2 表单弹窗模板

```vue
<template>
  <el-dialog v-model="visible" :title="isEdit ? '编辑' : '创建'" width="600px">
    <el-form :model="form" :rules="rules" ref="formRef" label-width="100px">
      <el-form-item label="名称" prop="name">
        <el-input v-model="form.name" />
      </el-form-item>
    </el-form>
    <template #footer>
      <el-button @click="visible = false">取消</el-button>
      <el-button type="primary" @click="handleSubmit" :loading="submitting">确定</el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, reactive, watch } from 'vue'

const props = defineProps<{
  modelValue: boolean
  record?: any
}>()

const emit = defineEmits<{
  'update:modelValue': [value: boolean]
  success: []
}>()

const visible = computed({
  get: () => props.modelValue,
  set: (val) => emit('update:modelValue', val)
})

const isEdit = computed(() => !!props.record)
const formRef = ref()
const submitting = ref(false)
const form = reactive({ name: '' })

watch(() => props.record, (val) => {
  if (val) {
    Object.assign(form, val)
  } else {
    form.name = ''
  }
})

const handleSubmit = async () => {
  await formRef.value.validate()
  submitting.value = true
  try {
    if (isEdit.value) {
      await api.update(props.record.id, form)
    } else {
      await api.create(form)
    }
    ElMessage.success('操作成功')
    visible.value = false
    emit('success')
  } finally {
    submitting.value = false
  }
}
</script>
```

## 2. 服务前缀速查

| 服务 | 前缀 | 文档 |
|------|------|------|
| 用户服务 | `/api/user` | [04-用户模块对接](04-用户模块对接.md) |
| 课程服务 | `/api/course` | [05-课程模块对接](05-课程模块对接.md) |
| 考试服务 | `/api/exam` | [06-考试模块对接](06-考试模块对接.md) |
| 知识服务 | `/api/knowledge` | [07-知识模块对接](07-知识模块对接.md) |
| 资源服务 | `/api/resource` | [08-资源模块对接](08-资源模块对接.md) |
| AI 服务 | `/api/ai` | [09-AI模块对接](09-AI模块对接.md) |

## 3. 枚举值速查

### 用户相关

| 枚举 | 值 | 说明 |
|------|-----|------|
| UserType.STUDENT | 1 | 学生 |
| UserType.TEACHER | 2 | 教师 |
| UserType.ADMIN | 3 | 管理员 |
| UserStatus.NORMAL | 1 | 正常 |
| UserStatus.BAN | 0 | 禁用 |

### 课程相关

| 枚举 | 值 | 说明 |
|------|-----|------|
| CourseStatus.DRAFT | 0 | 草稿 |
| CourseStatus.PUBLISHED | 1 | 已发布 |
| CourseStatus.ARCHIVED | 2 | 已归档 |

### 题目相关

| 枚举 | 值 | 说明 |
|------|-----|------|
| QuestionType.SINGLE_CHOICE | 0 | 单选题 |
| QuestionType.MULTI_CHOICE | 1 | 多选题 |
| QuestionType.TRUE_FALSE | 2 | 判断题 |
| QuestionType.FILL_BLANK | 3 | 填空题 |
| QuestionType.SHORT_ANSWER | 4 | 简答题 |

### 资源相关

| 枚举 | 值 | 说明 |
|------|-----|------|
| ResourceType.IMAGE | 0 | 图片 |
| ResourceType.DOCUMENT | 1 | 文档 |
| ResourceType.VIDEO | 2 | 视频 |
| ResourceType.AUDIO | 3 | 音频 |

## 4. 错误码速查

| 错误码 | 含义 | 前端处理 |
|--------|------|----------|
| 0 | 成功 | 正常处理 |
| 40000 | 参数错误 | 显示表单错误 |
| 40100 | 未登录 | 跳转登录页 |
| 40101 | 无权限 | 显示无权访问 |
| 40400 | 资源不存在 | 显示 404 页面 |
| 50000 | 系统错误 | 提示稍后重试 |

## 5. 路由权限配置速查

```typescript
// 公共页面（无需登录）
{ path: '/courses', component: CourseList }

// 需要登录
{ path: '/profile', component: Profile, meta: { requireAuth: true } }

// 仅学生
{ path: '/exams', component: ExamList, meta: { requireAuth: true, roles: [1] } }

// 教师+管理员
{ path: '/teacher/courses', component: TeacherCourse, meta: { requireAuth: true, roles: [2, 3] } }

// 仅管理员
{ path: '/admin/users', component: UserManage, meta: { requireAuth: true, roles: [3] } }
```

## 6. 文件路径速查

| 类型 | 路径 |
|------|------|
| API 封装 | `src/api/*.ts` |
| 类型定义 | `src/types/*.ts` |
| 公共组件 | `src/components/*.vue` |
| 组合式函数 | `src/composables/*.ts` |
| 页面组件 | `src/views/**/*.vue` |
| 状态管理 | `src/stores/*.ts` |
| 路由配置 | `src/router/*.ts` |
| 工具函数 | `src/utils/*.ts` |
| 静态资源 | `src/assets/**/*` |

## 7. 常用依赖安装

```bash
# 核心框架
npm install vue@^3.4 vue-router@^4 pinia@^2

# UI 组件库
npm install element-plus@^2

# HTTP 客户端
npm install axios@^1.6

# 工具库
npm install lodash-es vueuse

# Markdown 渲染（AI 聊天）
npm install marked highlight.js

# 开发依赖
npm install -D typescript vite@^5 @vitejs/plugin-vue
npm install -D sass vue-tsc
```

## 8. 开发检查清单

在生成前端代码时，请确认以下事项：

- [ ] 使用了正确的服务前缀（`/api/user`、`/api/course` 等）
- [ ] 需要登录的接口已添加 `requireAuth: true`
- [ ] 角色权限控制正确（学生/教师/管理员）
- [ ] 表单使用了正确的 TypeScript 类型
- [ ] 分页参数使用 `current` 和 `pageSize`（从 1 开始）
- [ ] 图片/文件上传使用了预签名流程
- [ ] AI 聊天使用了 SSE 流式处理
- [ ] 错误处理包含 40100（未登录）和 40101（无权限）
- [ ] 使用了 Element Plus 的组件和样式
- [ ] 组件命名使用 PascalCase，文件命名与组件名一致
