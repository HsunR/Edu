# 07 - 入口与配置重构

## 一、当前问题

### 1.1 `src/main.js` 问题

| # | 问题 | 严重程度 |
|---|------|---------|
| 1 | 使用 JS 而非 TS | 🟡 |
| 2 | 使用旧版 `import store from './store'` | 🔴 |
| 3 | 大量 `app.config.globalProperties` 挂载 | 🟡 |
| 4 | 手动注册全局组件 | 🟢 |
| 5 | 缺少 Pinia 持久化插件注册 | 🟡 |

### 1.2 `src/settings.js` 问题

| # | 问题 | 严重程度 |
|---|------|---------|
| 1 | 使用 JS 而非 TS | 🟡 |
| 2 | 版权信息硬编码为 RuoYi | 🟢 |
| 3 | 无类型定义 | 🟡 |

### 1.3 `vite.config.js` 问题

| # | 问题 | 严重程度 |
|---|------|---------|
| 1 | 使用 JS 而非 TS | 🟢 |
| 2 | `base` 配置两个分支相同 | 🟢 |
| 3 | 同时引入 Element Plus 和 Ant Design Vue | 🟡 |
| 4 | 代理只配了 `/api` | 🟡 |
| 5 | 开发服务器端口 80 需要管理员权限 | 🟡 |
| 6 | `charset-removal` PostCSS 插件暴力删除 | 🟢 |
| 7 | AutoImport 未配置 Vue/VueRouter/Pinia 自动导入 | 🟡 |

### 1.4 `tsconfig.app.json` 问题

| # | 问题 | 严重程度 |
|---|------|---------|
| 1 | `strict: true` 但 `noUnusedLocals: false` | 🟡 |
| 2 | 未包含 `.js` 文件 | 🟢 |

---

## 二、重构方案

### 2.1 `src/main.ts`

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'
import ElementPlus from 'element-plus'
import locale from 'element-plus/es/locale/lang/zh-cn'

import App from './App.vue'
import router from './router'
import './router/guards'
import { setupDirectives } from './directives'
import { setupGlobalComponents } from './components/setup'

import 'element-plus/dist/index.css'
import 'element-plus/theme-chalk/dark/css-vars.css'
import '@/assets/styles/index.scss'
import 'virtual:svg-icons-register'

const app = createApp(App)

// Pinia
const pinia = createPinia()
pinia.use(piniaPluginPersistedstate)
app.use(pinia)

// Router
app.use(router)

// Element Plus
app.use(ElementPlus, {
  locale,
  size: localStorage.getItem('size') || 'default',
})

// Directives
setupDirectives(app)

// Global Components
setupGlobalComponents(app)

app.mount('#app')
```

**关键变更**：
- ❌ 删除 `app.config.globalProperties` 所有挂载
- ❌ 删除 `import store from './store'`（旧版 Store）
- ✅ 使用 Pinia 持久化插件
- ✅ 路由守卫从 `./router/guards` 导入

### 2.2 `src/settings.ts`

```typescript
export interface AppSettings {
  title: string
  theme: string
  sideTheme: string
  showSettings: boolean
  topNav: boolean
  tagsView: boolean
  tagsIcon: boolean
  fixedHeader: boolean
  sidebarLogo: boolean
  dynamicTitle: boolean
  footerVisible: boolean
  footerContent: string
}

const settings: AppSettings = {
  title: import.meta.env.VITE_APP_TITLE || '智慧教育平台',
  theme: '#409EFF',
  sideTheme: 'theme-dark',
  showSettings: false,
  topNav: false,
  tagsView: true,
  tagsIcon: false,
  fixedHeader: false,
  sidebarLogo: true,
  dynamicTitle: false,
  footerVisible: true,
  footerContent: 'Copyright (C) 2024-2026 Intelli-Edu. All Rights Reserved.',
}

export default settings
```

### 2.3 `vite.config.ts`

```typescript
import { defineConfig, loadEnv } from 'vite'
import path from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'
import { createSvgIconsPlugin } from 'vite-plugin-svg-icons'

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd())

  return {
    base: '/',
    plugins: [
      createSvgIconsPlugin({
        iconDirs: [path.resolve(process.cwd(), 'src/assets/icons/svg')],
        symbolId: 'icon-[dir]-[name]',
      }),
      AutoImport({
        imports: ['vue', 'vue-router', 'pinia'],
        resolvers: [ElementPlusResolver()],
        dts: 'src/types/auto-imports.d.ts',
      }),
      Components({
        resolvers: [ElementPlusResolver()],
        dts: 'src/types/components.d.ts',
      }),
    ],
    resolve: {
      alias: {
        '~': path.resolve(__dirname, './'),
        '@': path.resolve(__dirname, './src'),
      },
      extensions: ['.mjs', '.js', '.ts', '.jsx', '.tsx', '.json', '.vue'],
    },
    build: {
      sourcemap: mode !== 'production',
      outDir: 'dist',
      assetsDir: 'assets',
      chunkSizeWarningLimit: 2000,
      rollupOptions: {
        output: {
          chunkFileNames: 'static/js/[name]-[hash].js',
          entryFileNames: 'static/js/[name]-[hash].js',
          assetFileNames: 'static/[ext]/[name]-[hash].[ext]',
        },
      },
    },
    server: {
      port: 5173,
      host: true,
      open: true,
      proxy: {
        '/api': {
          target: 'http://localhost:8890',
          changeOrigin: true,
        },
      },
    },
  }
})
```

**关键变更**：
- ❌ 删除 `AntDesignVueResolver`
- ✅ AutoImport 添加 `vue`/`vue-router`/`pinia` 自动导入
- ✅ 开发端口改为 `5173`
- ❌ 删除 `charset-removal` PostCSS 插件
- ✅ `base` 简化为 `'/'`

### 2.4 `tsconfig.app.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "types": ["vite/client"]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "src/types/auto-imports.d.ts",
    "src/types/components.d.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
```

**关键变更**：
- ✅ `noUnusedLocals: true`
- ✅ `noUnusedParameters: true`
- ✅ 更新 `auto-imports.d.ts` 路径

---

## 三、迁移步骤

### Step 1：重命名 `main.js` → `main.ts`

### Step 2：重写 `main.ts`

### Step 3：重命名 `settings.js` → `settings.ts` 并重写

### Step 4：重命名 `vite.config.js` → `vite.config.ts` 并重写

### Step 5：更新 `tsconfig.app.json`

### Step 6：删除 `globalProperties` 相关代码

搜索所有使用 `proxy.download`、`proxy.parseTime` 等全局方法的地方，替换为直接导入：

| 旧版 | 新版 |
|------|------|
| `proxy.download(url, params, filename)` | `import { download } from '@/api/request'` |
| `proxy.parseTime(time, format)` | `import { parseTime } from '@/utils'` |
| `proxy.resetForm(formRef)` | `formRef.value?.resetFields()` |
| `proxy.handleTree(data, id, parentId, children)` | `import { handleTree } from '@/utils'` |

### Step 7：验证

- [ ] 应用正常启动
- [ ] Element Plus 组件正常渲染
- [ ] Pinia 持久化正常
- [ ] 自动导入正常
