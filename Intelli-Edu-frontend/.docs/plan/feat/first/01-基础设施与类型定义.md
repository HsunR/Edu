# Phase 0 - 基础设施与类型定义

> 本阶段是所有后续开发的前提，必须首先完成。

## 1. TypeScript 类型定义

### 1.1 通用 API 类型 `src/types/api.ts`

```typescript
export interface PageRequest {
  current?: number
  pageSize?: number
  sortField?: string
  sortOrder?: string
}

export interface PageResult<T> {
  records: T[]
  total: number
  size: number
  current: number
  pages: number
}
```

> **注意**：后端非分页接口直接返回数据对象，无 `code/msg/data` 外层包装。因此不定义 `ApiResponse<T>`。分页接口返回 `PageResult<T>`，非分页接口直接返回业务 VO。

### 1.2 枚举定义 `src/types/enums.ts`

```typescript
export enum UserType {
  Student = 'Student',
  Teacher = 'Teacher',
  Admin = 'Admin'
}

export enum Sex {
  Unknown = '未知',
  Male = '男',
  Female = '女'
}

export enum UserStatus {
  Normal = '正常',
  Banned = '禁止'
}

export enum RegisterType {
  Mobile = '手机验证码注册',
  Email = '邮箱验证码注册',
  Wechat = '微信OpenID注册'
}

export enum CourseStatus {
  Draft = 0,
  Published = 1,
  Archived = 2
}

export enum ClassStatus {
  Enrolling = 0,
  InProgress = 1,
  Ended = 2
}

export enum ExamType {
  Exam = 0,
  Practice = 1,
  Homework = 2
}

export enum ExamStatus {
  NotStarted = 0,
  InProgress = 1,
  Ended = 2,
  Graded = 3
}

export enum QuestionType {
  SingleChoice = 0,
  MultipleChoice = 1,
  TrueFalse = 2,
  FillBlank = 3,
  ShortAnswer = 4
}

export enum ResourceType {
  Video = 1,
  Document = 2,
  Image = 3
}

export enum SectionResourceType {
  Video = 'VIDEO',
  Document = 'DOCUMENT',
  Image = 'IMAGE'
}

export enum UploadStatus {
  Pending = 0,
  Success = 1,
  Failed = 2
}

export enum GradingStatus {
  NotGraded = 0,
  Graded = 1,
  AIGrading = 2
}

export enum PaperStatus {
  Draft = 0,
  Published = 1
}

export enum SheetStatus {
  NotStarted = 0,
  InProgress = 1,
  Ended = 2,
  Graded = 3
}
```

> **关键**：`SectionResourceType` 与 `ResourceType` 是不同枚举！节资源关联用大写字符串（`"VIDEO"`），资源本身用整数（`1`）。

### 1.3 各模块类型定义

每个模块的类型定义放在 `src/api/{module}/types.ts` 中，具体类型见各模块开发计划。

## 2. API 请求层重构

### 2.1 当前问题

现有 `src/utils/request.js` 基于 axios 封装，但：
- 缺少 TypeScript 类型
- Token 刷新逻辑需要适配后端 `/api/user/auth/refresh-token` 接口
- 错误处理需要统一
- **缺少 BigInt 精度处理**：后端 ID 字段为 `int64`，JavaScript Number 超过 `2^53` 会丢失精度。现有代码使用 `JSONbigString.parse(data)` 处理，重构后必须保留
- Token 存储在 Cookie 中，需迁移到 localStorage

### 2.2 重构目标

**`src/utils/request.ts`**：

```typescript
import axios, { AxiosInstance, InternalAxiosRequestConfig, AxiosResponse } from 'axios'
import JSONbig from 'json-bigint'
import { getToken, getRefreshToken, setToken, setRefreshToken, removeToken } from '@/utils/auth'

const BASE_URL = import.meta.env.VITE_API_BASE_URL || ''

const JSONbigString = JSONbig({ storeAsString: true })

const service: AxiosInstance = axios.create({
  baseURL: BASE_URL,
  timeout: 30000,
  transformResponse: [
    (data) => {
      if (typeof data === 'string') {
        try {
          return JSONbigString.parse(data)
        } catch {
          return data
        }
      }
      return data
    }
  ]
})

let isRefreshing = false
let pendingRequests: Array<(token: string) => void> = []

service.interceptors.request.use((config: InternalAxiosRequestConfig) => {
  const token = getToken()
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

service.interceptors.response.use(
  (response: AxiosResponse) => {
    return response.data
  },
  async (error) => {
    const originalRequest = error.config
    if (error.response?.status === 401 && !originalRequest._retry) {
      if (isRefreshing) {
        return new Promise((resolve) => {
          pendingRequests.push((token: string) => {
            originalRequest.headers.Authorization = `Bearer ${token}`
            resolve(service(originalRequest))
          })
        })
      }
      originalRequest._retry = true
      isRefreshing = true
      try {
        const refreshToken = getRefreshToken()
        const res = await axios.post(`${BASE_URL}/api/user/auth/refresh-token`, null, {
          params: { refreshToken }
        })
        const data = res.data.data || res.data
        setToken(data.accessToken)
        setRefreshToken(data.refreshToken)
        pendingRequests.forEach(cb => cb(data.accessToken))
        pendingRequests = []
        originalRequest.headers.Authorization = `Bearer ${data.accessToken}`
        return service(originalRequest)
      } catch {
        removeToken()
        window.location.href = '/login'
        return Promise.reject(error)
      } finally {
        isRefreshing = false
      }
    }
    return Promise.reject(error)
  }
)

export default service
```

### 2.3 Token 存储迁移 `src/utils/auth.ts`

```typescript
const ACCESS_TOKEN_KEY = 'intelli_edu_access_token'
const REFRESH_TOKEN_KEY = 'intelli_edu_refresh_token'

export function getToken(): string | null {
  return localStorage.getItem(ACCESS_TOKEN_KEY)
}

export function setToken(token: string): void {
  localStorage.setItem(ACCESS_TOKEN_KEY, token)
}

export function getRefreshToken(): string | null {
  return localStorage.getItem(REFRESH_TOKEN_KEY)
}

export function setRefreshToken(token: string): void {
  localStorage.setItem(REFRESH_TOKEN_KEY, token)
}

export function removeToken(): void {
  localStorage.removeItem(ACCESS_TOKEN_KEY)
  localStorage.removeItem(REFRESH_TOKEN_KEY)
}
```

> **迁移说明**：现有代码使用 Cookie 存储 `Admin-Token`，需迁移到 localStorage。原因：
> 1. 避免 CSRF 攻击
> 2. 需要同时存储 `accessToken` 和 `refreshToken`
> 3. 跨服务共享 Token 更方便

### 2.4 Vite 代理配置补全

现有 `vite.config.ts` 缺少 exam 和 knowledge 服务代理，需添加：

```typescript
server: {
  proxy: {
    '/api/user': {
      target: 'http://192.168.101.1:8891',
      changeOrigin: true
    },
    '/api/resource': {
      target: 'http://192.168.101.1:8892',
      changeOrigin: true
    },
    '/api/course': {
      target: 'http://192.168.101.1:8893',
      changeOrigin: true
    },
    '/api/exam': {
      target: 'http://192.168.101.1:8894',
      changeOrigin: true
    },
    '/api/knowledge': {
      target: 'http://192.168.101.1:8895',
      changeOrigin: true
    },
    '/api/ai': {
      target: 'http://192.168.101.1:8896',
      changeOrigin: true
    }
  }
}
```

### 2.5 API 模块组织规范

每个微服务对应一个 API 目录：

```
src/api/
  user/
    types.ts     # 请求/响应类型
    auth.ts      # 鉴权相关接口
    user.ts      # 用户信息接口
    index.ts     # 统一导出
  course/
    types.ts
    category.ts
    course.ts
    chapter.ts
    section.ts
    class.ts
    index.ts
  exam/
    types.ts
    questionBank.ts
    question.ts
    paper.ts
    exam.ts
    answer.ts
    index.ts
  knowledge/
    types.ts
    point.ts
    index.ts
  resource/
    types.ts
    resource.ts
    index.ts
  ai/
    types.ts
    chat.ts
    index.ts
```

### 2.6 API 函数编写规范

```typescript
import request from '@/utils/request'
import type { PageResult, PageRequest } from '@/types/api'
import type { CourseVO, CourseDetailVO, CourseCreateRequest, CourseUpdateRequest, CourseQueryRequest } from './types'

export function getCourseList(params: CourseQueryRequest) {
  return request.get<any, PageResult<CourseVO>>('/api/course/courses', { params })
}

export function getCourseDetail(courseId: number) {
  return request.get<any, CourseDetailVO>(`/api/course/courses/${courseId}`)
}

export function createCourse(data: CourseCreateRequest) {
  return request.post<any, CourseVO>('/api/course/courses', data)
}

export function updateCourse(courseId: number, data: CourseUpdateRequest) {
  return request.put<any, CourseVO>(`/api/course/courses/${courseId}`, data)
}

export function deleteCourse(courseId: number) {
  return request.delete(`/api/course/courses/${courseId}`)
}
```

**关键规则：**
- 函数名以 HTTP 动词前缀：`get` / `create` / `update` / `delete`
- 路径参数用模板字符串
- 查询参数用 `params`，请求体用 `data`
- 必须标注返回类型
- **int64 ID 在 TS 中用 `number` 类型**（因为 `json-bigint` 的 `storeAsString` 模式将大整数转为字符串，但前端展示和传参时作为 number 使用不会超出安全范围；若后续 ID 增长超出安全范围，需改用 `string` 类型）

## 3. 路由与权限守卫

### 3.1 路由结构

```
/login                    # 登录页
/register                 # 注册页
/                         # 首页（课程浏览）
/course/:courseId         # 课程详情
/learning/:courseId       # 学生学习页
/learning/:courseId/section/:sectionId  # 章节学习
/teaching/:courseId       # 教师教学管理页
/teaching/:courseId/settings      # 课程设置
/teaching/:courseId/chapters      # 章节管理
/teaching/:courseId/classes       # 班级管理
/teaching/:courseId/exams         # 考试管理
/teaching/:courseId/question-bank # 题库管理
/teaching/:courseId/knowledge    # 知识点管理
/teaching/:courseId/resources    # 资源管理
/exam/:examId/answer      # 答题页
/exam/:examId/result      # 答题结果
/profile                  # 个人信息
/admin                    # 管理后台
/ai/chat                  # AI 对话
```

### 3.2 权限守卫

在 `src/permission.ts` 中：
- 未登录 → 跳转 `/login`
- Token 过期 → 自动刷新
- 角色权限 → 检查 `userType` 是否匹配路由要求的角色
- 现有 `v-hasPermi` 和 `v-hasRole` 指令保留并适配新的 userType 枚举

## 4. 状态管理（Pinia Stores）

### 4.1 必需的 Stores

| Store | 文件 | 说明 |
|-------|------|------|
| useUserStore | `stores/user.ts` | 用户信息、Token、角色 |
| useAppStore | `stores/app.ts` | 应用全局状态（侧边栏、主题等） |
| useCourseStore | `stores/course.ts` | 当前课程上下文、分类树缓存 |
| useExamStore | `stores/exam.ts` | 答题状态（计时、答案暂存） |

### 4.2 useUserStore 核心逻辑

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { login as loginApi, logout as logoutApi, getUserInfo } from '@/api/user'
import { getToken, setToken, removeToken, getRefreshToken, setRefreshToken } from '@/utils/auth'
import type { LoginRequest, UserDetailVO } from '@/api/user/types'
import { UserType } from '@/types/enums'

export const useUserStore = defineStore('user', () => {
  const token = ref(getToken() || '')
  const refreshToken = ref(getRefreshToken() || '')
  const userInfo = ref<UserDetailVO | null>(null)

  const userType = computed(() => userInfo.value?.type)
  const isStudent = computed(() => userType.value === UserType.Student)
  const isTeacher = computed(() => userType.value === UserType.Teacher)
  const isAdmin = computed(() => userType.value === UserType.Admin)
  const isLoggedIn = computed(() => !!token.value)

  async function login(data: LoginRequest) {
    const res = await loginApi(data)
    token.value = res.accessToken
    refreshToken.value = res.refreshToken
    setToken(res.accessToken)
    setRefreshToken(res.refreshToken)
  }

  async function fetchUserInfo() {
    userInfo.value = await getUserInfo()
  }

  async function logout() {
    try {
      await logoutApi()
    } finally {
      token.value = ''
      refreshToken.value = ''
      userInfo.value = null
      removeToken()
    }
  }

  return { token, refreshToken, userInfo, userType, isStudent, isTeacher, isAdmin, isLoggedIn, login, fetchUserInfo, logout }
})
```

### 4.3 Store 迁移说明

现有 `src/store/modules/` 目录下的 store 需迁移到 `src/stores/`：
- `user.ts` → 重构为上述 useUserStore
- `app.ts` → 保留侧边栏/主题逻辑
- `settings.ts` → 合并到 app store
- `permission.ts` → 迁移路由权限逻辑到 `permission.ts` 文件

## 5. 开发检查清单

- [ ] `src/types/api.ts` - 通用 API 类型（不含 ApiResponse）
- [ ] `src/types/enums.ts` - 枚举定义（含 SectionResourceType）
- [ ] `src/utils/request.ts` - axios 封装 + BigInt 处理 + Token 刷新
- [ ] `src/utils/auth.ts` - Token 存取（localStorage，从 Cookie 迁移）
- [ ] `vite.config.ts` - 补全 exam/knowledge 代理
- [ ] `src/stores/user.ts` - 用户 Store（从 store/modules/ 迁移）
- [ ] `src/stores/app.ts` - 应用 Store
- [ ] `src/permission.ts` - 路由守卫
- [ ] `src/router/index.ts` - 路由配置
- [ ] 各模块 `src/api/{module}/types.ts` - 类型定义
- [ ] 各模块 `src/api/{module}/*.ts` - API 函数
